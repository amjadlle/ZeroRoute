services:
  zeroroute:
    build: .
    container_name: zeroroute
    restart: unless-stopped
    ports:
      - "8787:8787"
    env_file:
      - .env
    volumes:
      - ./config.json:/app/config.json
